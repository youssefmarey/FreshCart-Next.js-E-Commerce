# Base API for backend auth endpoints
API=https://ecommerce.routemisr.com/api/v1

# NextAuth config
NEXTAUTH_URL=http://localhost:3000
# Generate a strong secret: openssl rand -base64 32 (or let Vercel set it)
NEXTAUTH_SECRET=changeme_in_prod
